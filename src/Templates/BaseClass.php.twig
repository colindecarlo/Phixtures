<?php

class {{ name }}
{
	
	{% for column, details in columns -%}
	{% if details.nullable and details.default == null -%}
		public {{ column }} = null;
	{% else -%}
		public {{ column }};
	{% endif -%}
	{%- endfor %}

	public function __construct()
	{
		{% for column, details in columns -%}
		{% if details.default != null -%}
			$this->{{ column }} = '{{ details.default }}';
		{% endif -%}
		{%- endfor %}

		$this->init();
	}

	public function init()
	{
	}
}
